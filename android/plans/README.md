# Animation Plans — android/

Self-contained implementation plans generated by `improve-animations`. Each plan is written for an executor with zero context: exact file paths, current-code excerpts, target values, and feel-check steps.

| # | Plan | Severity | Category | Status |
|---|---|---|---|---|
| 001 | [Done completion emphasis on StatusPill](001-done-completion-emphasis.md) | MEDIUM | Missed opportunities (delight) | DONE |

## Recommended execution order

1. **001** — done completion emphasis (self-contained; touches `StatusPill.kt` only).

No dependencies between plans. This plan extends the existing Compose animation conventions (`animate*AsState`, `shouldReduceMotion()` → `snap()`, `tween`/`spring` from `androidx.compose.animation.core`) rather than introducing a motion library or new easing tokens.

## Notes

- The other two rows from the `find-animation-opportunities` sweep (empty↔content crossfade in `DashboardScreen`/`CatDetailScreen`, and `animateItem` reflow in `CatDetailScreen`) were not yet requested as plans; add them as 002/003 when selected.
- Reduced-motion handling is already established repo-wide via `shouldReduceMotion()`; every plan must gate its motion through it with `snap()` as the reduced alternative.
